#include<stdio.h>
#include<conio.h>
void main()
{
int n;
printf("enter the no.");
scanf("%d",&n);
if(n>0)
printf("the no. is positive");
if(n<0)
printf("the no is negative");
if(n==0)
printf("the number is zero");
getch();
}
